source 'http://gemcutter.org'
source 'http://gems.github.com'

gem 'activesupport'
gem 'dm-core'
gem 'dm-types'
gem 'dm-is-list'
gem 'haml'
gem 'less'
gem 'sinatra'

only :test do
  gem 'cucumber'
  gem 'faker'
  gem 'jspec',      :lib => false
  gem 'machinist'
  gem 'rspec',      :lib => 'spec'
  gem 'webrat'
end

# only :cucumber do
#   gem 'cucumber', '>=0.4.3', :lib => false unless directory?('vendor/plugins/cucumber')
#   gem 'webrat', '>=0.5.3', :lib => false unless directory?('vendor/plugins/webrat')
#   gem 'rspec', '>=1.2.9', :lib => false unless directory?('vendor/plugins/rspec')
# end

