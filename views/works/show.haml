%section#work_details
  %header
    %h1
      :textileinline
        #{ @work.title }
  .description
    :textile
      #{ @work.description }
  %ul.images
    = partial :'works/work_image', :collection => @work.images