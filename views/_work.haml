.work
  %h3.title
    %a{ :href => "/works/#{ work.id }" }
      :textileinline
        #{ work.title }
  .description
    :textile
      #{ work.description }
  %ul.images
    = partial :work_image, :collection => work.images